<%= form_for(beer_main) do |f| %>
  <% if beer_main.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_main.errors.count, "error") %> prohibited this beer_main from being saved:</h2>

      <ul>
      <% beer_main.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
